import matplotlib.pyplot as plt
from datetime import datetime
from matplotlib.animation import FuncAnimation


def update_graph(classifications, axis, classes):
    # axis.clear()
    # axis.plot(classifications, label='Classification', color='blue')
    # axis.set_title('Real-Time Classification Results')
    # axis.set_xlabel('Frame Number')
    # axis.set_ylabel('Class Index')
    # axis.set_ylim(-0.5, len(classes) - 0.5)  # Adjust according to your classes
    # axis.set_xticks(range(0, len(classifications), max(1, len(classifications) // 10)))  # Show fewer ticks for clarity
    # axis.set_xticklabels(range(len(classifications)), rotation=45)
    # axis.yaxis.grid(True, linestyle='--', alpha=0.7)
    # axis.legend()
    # plt.tight_layout()  # Ensure tight layout
    pass
    


